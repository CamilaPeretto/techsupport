// Re-export do modal de atualização de status (comentado)
// Este arquivo apenas re-exporta o componente padrão do modal para importações mais simples
import StatusUpdateModal from './StatusUpdateModal';
export default StatusUpdateModal;
